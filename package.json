{
	"name": "react-chat-app-2",
	"version": "1.0.0",
	"main": "index.js",
	"license": "MIT",
	"engines": {
		"node": "6.11.3"
	},
	"dependencies": {
		"concurrently": "^3.5.0"
	},
	"scripts": {
		"build": "concurrently \"cd client && yarn build && npm install --only=dev\"",
		"start": "concurrently \"cd client && PORT=3000 yarn start\" \"cd server && PORT=3001 yarn start\"",
		"heroku-postbuild": "yarn build"
	}
}
